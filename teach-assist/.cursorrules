{
  "rules": {
    "first_rule": "DO NOT MODIFY THE .cursorrules file unless clearly specified",
    
    "general_instructions": [
      "All modifications should be made only in the requested section",
      "Create separate endpoints in ai_backend for each tool/page, verify with user if similar"
    ],

    "module_responsibilities": {
      "ai_backend": {
        "responsibility": "OpenAI API interactions only",
        "allowed_operations": ["OpenAI API calls", "Request handling", "Response streaming"],
        "forbidden_operations": ["Database operations", "File storage", "Authentication"],
        "models": {
          "default": "gpt-4o-mini",
          "complex_tasks": "gpt-4o",
          "forbidden": ["gpt-4"]
        }
      },
      "db_backend": {
        "responsibility": "Database operations only",
        "allowed_operations": ["MongoDB operations", "Data validation", "Authentication"],
        "forbidden_operations": ["OpenAI API calls", "AI model interactions"]
      },
      "client": {
        "responsibility": "User interface and interactions"
      }
    },

    "technology_stack": {
      "ai_backend": ["Python", "FastAPI", "OpenAI API"],
      "db_backend": ["Node.js", "Express", "MongoDB"],
      "client": ["React", "Tailwind CSS", "Vite"]
    },

    "file_structure": {
      "ai_backend": {
        "main.py": "Main FastAPI application",
        "config.py": "Configuration settings",
        "app.py": "Application logic",
        "lesson_plan_generator.py": "Lesson plan generation logic",
        "intervention_note_improver.py": "Note improvement logic",
        "requirements.txt": "Python dependencies",
        ".env": "Environment variables",
        "vercel.json": "Vercel deployment config"
      },
      "db_backend": {
        "server.js": "Main Express application",
        "api/": "API routes",
        "models/": "MongoDB models",
        "validators/": "Request validation",
        "middleware/": "Express middleware",
        "config/": "Configuration files",
        "package.json": "Node.js dependencies",
        ".env": "Environment variables",
        "vercel.json": "Vercel deployment config"
      },
      "client": {
        "src/": "React source code",
        "public/": "Static assets",
        "build/": "Production build",
        "package.json": "Node.js dependencies",
        "tsconfig.json": "TypeScript config",
        "webpack.config.js": "Webpack config",
        ".env.development": "Development env vars",
        ".env.production": "Production env vars"
      }
    },

    "error_handling": [
      "Try to fix errors with concise solutions",
      "Ask user for clarification if unable to fix"
    ]
  }
}

