// Types/GuidedReadingSession.ts

export interface GuidedReadingSession {
  _id: string;
  date: string;
  book: string;
  level: string;
  nextSteps: string;
  groupId: string;
  teacherId: string;
}
